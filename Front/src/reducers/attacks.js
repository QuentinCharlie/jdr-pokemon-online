// Action Types

// Initial State
const initialState = {
};

// Reducer
const attacksReducer = (state = initialState, action = {}) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default attacksReducer;
